/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',      // required to generate /out folder
  reactCompiler: true,   // keeps your existing config
};

export default nextConfig;
